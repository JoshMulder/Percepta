# Pins deliberately match DroneOps where the dependency is shared, so patterns
# and workarounds carry across without version drift between the two codebases.
# All of these install and import cleanly on Python 3.14.
fastapi==0.139.0
uvicorn[standard]==0.50.2
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
passlib[bcrypt]==1.7.4
# Held at 4.0.1: passlib 1.7.4 reads bcrypt.__about__.__version__, which bcrypt
# removed in 4.1. Same pin, same reason, as DroneOps.
bcrypt==4.0.1
PyJWT==2.13.0
cryptography==49.0.0
pyotp==2.10.0
segno==1.6.6
# Control leases, live fan-out between API processes, and revocation push.
redis==5.2.1
httpx==0.28.1
# Demo airband source: resampling and noise generation for the simulated
# receiver (services/airband_demo.py). Not used on the request path.
numpy==2.2.1
