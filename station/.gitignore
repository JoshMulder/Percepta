# Station state: credential, device inventory, recordings, event database.
# The credential in particular must never be committed.
var/
.venv/
__pycache__/
*.pyc

# Wheels staged for an offline install on a box with no network.
deploy/wheels/
