{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://percepta/contract/v1/telemetry.schema.json",
  "contractVersion": "1.0",
  "title": "Ground station telemetry",
  "description": "One object per message on gsu/{station_id}/telemetry. Discriminated by `kind`. Unknown fields are ignored by the platform, and unknown kinds are dropped rather than erroring - so a new sensor can be added station-side before the platform renders it.\n\nEvery stream shares the availability fields in $defs/availability: a payload may carry `available: false` to declare that the station has no source for that stream, and its usual fields are then not required, because there is nothing to put in them.",
  "type": "object",
  "required": [
    "kind"
  ],
  "oneOf": [
    {
      "$ref": "#/$defs/adsb"
    },
    {
      "$ref": "#/$defs/weather"
    },
    {
      "$ref": "#/$defs/power"
    },
    {
      "$ref": "#/$defs/radio"
    },
    {
      "$ref": "#/$defs/light"
    },
    {
      "$ref": "#/$defs/health"
    }
  ],
  "$defs": {
    "availability": {
      "description": "Fields every stream carries, defined once. They exist because an empty payload and a dead sensor are otherwise indistinguishable, and only the station knows which it is: an empty `aircraft` array means clear airspace, and a station with no ADS-B receiver must not be able to say that.",
      "properties": {
        "available": {
          "type": "boolean",
          "default": true,
          "description": "False when the station has no source for this stream. Absent means true, so nothing that predates this field changes meaning. A consumer that does not know the field sees a payload missing its usual contents and can ignore it, exactly as it ignores an unknown kind."
        },
        "unavailable_reason": {
          "type": "string",
          "maxLength": 200,
          "description": "Short, human-readable, for an operator rather than a parser - 'no ADS-B receiver connected', 'weather station not responding'. Only meaningful when available is false."
        },
        "unavailable_cause": {
          "enum": [
            "not_fitted",
            "not_detected",
            "stopped"
          ],
          "description": "The same fact as unavailable_reason, in one word a consumer can branch on. not_fitted is a complete station with nothing selected for this slot and is not a fault; not_detected and stopped both are. Carried on the stream rather than only in health because a console that has just connected or switched station would otherwise wait a full health period - 30 seconds - to tell an empty slot from a broken sensor, and must show something in the meantime. Absent means unknown, which consumers should treat as they did before this field existed."
        },
        "simulated": {
          "type": "boolean",
          "description": "This stream's source is a demo sensor and its readings are synthetic. Per stream, not per station: a station is routinely part real - a bench box with a live camera and a demo weather head - and a station-wide flag had to be wrong about one half of it. Absent means real. Distinct from the per-contact simulated flag inside an aircraft, which means a test target injected by a genuine receiver."
        }
      }
    },
    "declared_unavailable": {
      "description": "The condition each stream's `else: required` hangs off: the station has said this stream has no source, so its usual fields are not demanded.",
      "properties": {
        "available": {
          "const": false
        }
      },
      "required": [
        "available"
      ]
    },
    "adsb": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "adsb"
        },
        "aircraft": {
          "type": "array",
          "maxItems": 500,
          "description": "The complete current picture, not a delta. An empty array means no contacts, which is different from sending nothing - sending nothing is what a failed receiver looks like.\n\nThis is the station's most expensive unconditional stream: at 1 Hz with no lease and no gate, a busy 50-contact picture is roughly 20 kB a frame, ~150 kbit/s, ~1.5 GB a day, and it is genuinely uncompressed on the wire. See transport.md's cadence section. The cap is far above any real receiver's traffic and exists so that one station cannot hand every console watching it an unbounded array to render; a station seeing more contacts than this should send the nearest 500, which is what an operator is looking at anyway. Omitting fields with no value is worth 10-20% and costs nothing - only icao, range_km and bearing are required.",
          "items": {
            "$ref": "#/$defs/aircraft"
          }
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/availability"
        },
        {
          "if": {
            "$ref": "#/$defs/declared_unavailable"
          },
          "else": {
            "required": [
              "aircraft"
            ]
          }
        }
      ]
    },
    "aircraft": {
      "type": "object",
      "properties": {
        "icao": {
          "type": "string",
          "description": "24-bit address, hex. The identity the platform tracks a contact by."
        },
        "callsign": {
          "type": [
            "string",
            "null"
          ]
        },
        "latitude": {
          "type": [
            "number",
            "null"
          ],
          "minimum": -90,
          "maximum": 90,
          "description": "Null until a position has been decoded; a Mode S response alone gives none."
        },
        "longitude": {
          "type": [
            "number",
            "null"
          ],
          "minimum": -180,
          "maximum": 180
        },
        "altitude": {
          "type": [
            "number",
            "null"
          ],
          "description": "Metres. Which datum this is depends on altitude_type; a pressure altitude is not height above the station's ground and can be several hundred feet out on a low-pressure day."
        },
        "track": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "exclusiveMaximum": 360,
          "description": "Degrees true. Null if unknown - the console draws a dot rather than pointing it in an invented direction."
        },
        "speed": {
          "type": [
            "number",
            "null"
          ],
          "description": "Knots."
        },
        "range_km": {
          "type": "number",
          "minimum": 0,
          "description": "From the station."
        },
        "bearing": {
          "type": "number",
          "minimum": 0,
          "exclusiveMaximum": 360,
          "description": "Degrees true, station to aircraft."
        },
        "alert": {
          "type": "boolean",
          "description": "Close and low enough to be worth flagging. The station decides, because the threshold belongs with the site."
        },
        "altitude_type": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "pressure",
            "geometric",
            null
          ],
          "description": "ADSB_ALTITUDE_TYPE. `pressure` is referenced to 1013.25 hPa, which is what most transponders send; `geometric` is GNSS height. Null when the receiver did not say."
        },
        "altitude_corrected_m": {
          "type": [
            "number",
            "null"
          ],
          "description": "The pressure altitude re-referenced to the station's own measured barometer, in metres. Present only when altitude_type is `pressure` and the station has a working barometric sensor; null otherwise. Never a substitute for `altitude` - both travel, so the console can show what was received and what it means locally."
        },
        "vertical_speed": {
          "type": [
            "number",
            "null"
          ],
          "description": "Metres per second, positive climbing. Null when the receiver's vertical-velocity flag was not set."
        },
        "emitter_type": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "maximum": 255,
          "description": "ADSB_EMITTER_TYPE as the receiver reported it, unmapped: 0 no info, 1 light, 2 small, 3 large, 4 high-vortex large, 5 heavy, 6 highly manoeuvrable, 7 rotorcraft, 9 glider, 10 lighter-than-air, 11 parachute, 12 ultralight, 14 UAV, 15 space, 17 emergency surface, 18 service surface, 19 point obstacle. The integer travels rather than a name because naming is a display decision and a receiver may report a value this list does not cover."
        },
        "squawk": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Mode A code as four octal digits carried as an integer, so 7700 is 7700. Null when the receiver's squawk flag was not set."
        },
        "seconds_since_contact": {
          "type": [
            "number",
            "null"
          ],
          "description": "ADSB_VEHICLE's tslc. How long since this contact was last heard from - a track still on the map at 30 seconds is a memory, not an aircraft."
        },
        "on_ground": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Reported airborne/surface state. Null when unknown."
        },
        "simulated": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "The receiver flagged this contact as simulated. Carried so a test injection can never be mistaken for traffic."
        },
        "source": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "adsb",
            "uat",
            null
          ],
          "description": "1090ES or UAT, from the receiver's source flag."
        }
      },
      "required": [
        "icao",
        "range_km",
        "bearing"
      ],
      "description": "One contact. A contact with no decoded position is NOT reported - transmitting position is the primary function of ADS-B, so a positionless return (a bare Mode S reply) is counted station-side and dropped rather than sent with nulls. range_km and bearing are therefore always present."
    },
    "weather": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "weather"
        },
        "wind_kt": {
          "type": "number",
          "minimum": 0
        },
        "gust_kt": {
          "type": "number",
          "minimum": 0
        },
        "wind_dir_deg": {
          "type": "number",
          "minimum": 0,
          "exclusiveMaximum": 360,
          "description": "Direction the wind comes FROM - meteorological convention, the opposite of a movement vector. Getting this backwards is the classic wind-rose bug."
        },
        "temperature_c": {
          "type": "number"
        },
        "humidity_pct": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Relative humidity, if the station has a sensor for it. Optional: the Airmar 110WX ships with and without the RH module, and requiring it would force a station without one to invent a number. Omit it rather than sending a placeholder."
        },
        "pressure_hpa": {
          "type": [
            "number",
            "null"
          ]
        },
        "visibility_km": {
          "type": [
            "number",
            "null"
          ]
        },
        "sky": {
          "enum": [
            "clear",
            "partly",
            "cloudy",
            "rain",
            "fog"
          ],
          "description": "Present weather, reported by the station rather than inferred by the console - so the icon can never contradict the numbers beside it."
        },
        "is_day": {
          "type": "boolean"
        },
        "rain_rate_mmh": {
          "type": "number",
          "minimum": 0
        },
        "rain_mm_today": {
          "type": "number",
          "minimum": 0,
          "description": "Running total since local midnight, in the station's own timezone."
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/availability"
        },
        {
          "if": {
            "$ref": "#/$defs/declared_unavailable"
          },
          "else": {
            "required": [
              "wind_kt",
              "gust_kt",
              "wind_dir_deg",
              "temperature_c"
            ]
          }
        }
      ]
    },
    "power": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "power"
        },
        "soc_pct": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Battery state of charge. Recorded to history at one sample per minute."
        },
        "battery_v": {
          "type": "number"
        },
        "pv_w": {
          "type": "number",
          "description": "In from the array."
        },
        "load_w": {
          "type": "number",
          "description": "Out to the load."
        },
        "runtime_h": {
          "type": [
            "number",
            "null"
          ],
          "description": "Hours left at the current draw; null while charging."
        },
        "battery_w": {
          "type": "number",
          "description": "Signed battery power in watts: positive charging, negative discharging. Sent rather than left to be derived, because with four possible sources the console cannot work out the battery's direction from the others without knowing about conversion losses and which source is carrying the load. The station measures it; a consumer subtracting pv_w from load_w would be guessing."
        },
        "mains_w": {
          "type": "number",
          "description": "Watts drawn from the AC mains input. Present only when a mains input is fitted — a site with no grid connection omits this and mains_present entirely, the same way a weather head with no RH module omits humidity. Absent is 'no such source', which is different from 0 W."
        },
        "mains_present": {
          "type": "boolean",
          "description": "Whether the AC mains input has power. False on a fitted input means the grid is down, which is a fault worth acting on; the field being absent means there is no mains connection at this site and nothing is wrong. Collapsing the two would make every off-grid station look like it had lost power."
        },
        "generator_w": {
          "type": "number",
          "description": "Watts being delivered by the backup generator. Present only when one is fitted."
        },
        "generator_running": {
          "type": "boolean",
          "description": "Whether the generator is running. Present only when one is fitted. False and running-but-producing-nothing are different states: a generator that is running and delivering 0 W has started and failed to take the load, which is exactly the case somebody needs to be told about."
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/availability"
        },
        {
          "if": {
            "$ref": "#/$defs/declared_unavailable"
          },
          "else": {
            "required": [
              "soc_pct"
            ]
          }
        }
      ]
    },
    "radio": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "radio"
        },
        "freq_hz": {
          "type": "integer",
          "minimum": 108000000,
          "maximum": 137000000,
          "description": "Airband only. The console clamps to this range and so should the station."
        },
        "rssi_db": {
          "type": "number",
          "description": "In-channel signal, dBFS."
        },
        "noise_floor_db": {
          "type": "number",
          "description": "Measured OUTSIDE the channel - see contract/README.md rule 3."
        },
        "threshold_db": {
          "type": "number",
          "description": "Squelch threshold currently applied, whether set by hand or by AUTO."
        },
        "squelch_open": {
          "type": "boolean",
          "description": "True whenever audio is flowing, including when held open by monitor."
        },
        "auto_squelch": {
          "type": "boolean",
          "description": "Reported by the station, not remembered by the console - the AUTO chip reflects hardware state rather than the last click."
        },
        "monitor": {
          "type": "boolean",
          "description": "Squelch defeated by an operator holding monitor."
        },
        "gain": {
          "description": "Tuner gain: \"auto\" or dB. Prefer a fixed value; the tuner AGC desenses near strong transmitters.",
          "oneOf": [
            {
              "const": "auto"
            },
            {
              "type": "number"
            }
          ]
        },
        "gains": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "description": "Gains this hardware offers, so the console can list them rather than guess."
        },
        "ppm": {
          "type": "integer",
          "description": "Crystal correction currently applied."
        },
        "tx_capable": {
          "type": "boolean",
          "description": "False on receive-only hardware. The console disables PTT from this."
        },
        "spectrum": {
          "type": "array",
          "items": {
            "type": "integer"
          },
          "maxItems": 256,
          "description": "Per-bin power in dBFS across span_hz, centred on freq_hz, for a spectrum display. Present only while a console has asked for it (the radio.spectrum command): sending it continuously is on the order of 50-100 MB a day on a metered link, for a display that is open for minutes at commissioning. Decimated by peak rather than mean — an airband channel is a handful of bins wide and averaging buries a real carrier — and rounded to whole dB, which is all a canvas can render. (An earlier note here said 150 MB, which was the arithmetic for 241 bins as decimal floats; this field is whole-dB integers and the station decimates to 128 bins before sending.)"
        },
        "span_hz": {
          "type": "number",
          "description": "Total width the spectrum array covers, centred on freq_hz. Present only alongside spectrum."
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/availability"
        },
        {
          "if": {
            "$ref": "#/$defs/declared_unavailable"
          },
          "else": {
            "required": [
              "freq_hz",
              "rssi_db",
              "noise_floor_db",
              "threshold_db",
              "squelch_open",
              "auto_squelch",
              "tx_capable"
            ]
          }
        }
      ]
    },
    "light": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "light"
        },
        "on": {
          "type": "boolean",
          "description": "What the hardware is actually doing, not what was last commanded. The console renders this and never assumes a command succeeded."
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/availability"
        },
        {
          "if": {
            "$ref": "#/$defs/declared_unavailable"
          },
          "else": {
            "required": [
              "on"
            ]
          }
        }
      ]
    },
    "health": {
      "type": "object",
      "description": "How the station itself is doing, as distinct from what it is measuring. Sent on a slow cadence.\n\nThis is where the *structured* form of things the other streams can only say in prose lives. `available: false` on a stream carries a human-readable reason; `devices[].status` says the same thing in a form a console can branch on - a receiver that was never fitted and one that has stopped answering both make a stream unavailable, and an operator does very different things about each.\n\nDeliberately permissive. The station owns the truth about what is attached to it, so unknown fields here are expected rather than tolerated: a station that learns to report something new must not have to wait for the platform.",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "health"
        },
        "contract_version": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+$",
          "description": "Which version of this contract the station speaks, `major.minor`. **Declared, not negotiated**: the platform records it, logs a mismatch, and carries on - a station is never refused for it, because refusing a box on a hillside over a version string turns a compatibility note into a site visit.\n\nA major bump means something changed meaning or went away, and the two sides need looking at. A minor bump means fields or kinds were added, which both sides already tolerate by design. Absent means a station older than this field, which is the same as 1.0.\n\nIt exists because the first question of every remote debugging session is which contract that box is on, and until this field there was no way to answer it: config_version is site policy and agent_version is a build string."
        },
        "status": {
          "type": "string",
          "enum": [
            "ok",
            "degraded",
            "failing"
          ],
          "description": "The station's own summary of itself. `degraded` means it is still doing its job with something wrong - a sensor down, a renewal failing - and is the state that most needs to reach an operator before it becomes `failing`."
        },
        "conditions": {
          "type": "array",
          "maxItems": 64,
          "description": "Named problems currently true, e.g. `uplink.down`, `credential.renewal_failing`. Raised and cleared by the station; the platform surfaces them and never infers them. Bounded because a console raises an alert per condition it has not seen before and holds them for the session: a station is reporting its own faults here, and a station with more than a few dozen distinct ones is not reporting, it is flooding.",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "maxLength": 100
              },
              "severity": {
                "type": "string",
                "enum": [
                  "info",
                  "warning",
                  "critical"
                ]
              },
              "detail": {
                "type": "string",
                "maxLength": 500
              }
            },
            "required": [
              "id"
            ]
          }
        },
        "agent_version": {
          "type": "string"
        },
        "config_version": {
          "type": "integer",
          "description": "The configuration generation the station has actually applied, reported so the platform can record and display what is running. The platform does not currently push configuration - see enrolment.md §7 - so this is a display of fact, not half of a sync."
        },
        "uptime_s": {
          "type": "number",
          "minimum": 0
        },
        "position": {
          "type": ["object", "null"],
          "description": "Where this station says it is. **The station owns this** - enrolment sets the initial value, and from then on the box is the one place it is edited, because the person standing at the site is the one who knows. The platform stores what it is told and its console renders the position read-only; two editable copies of one fact disagree, and the disagreement is invisible from both ends.\n\nOnly ever the station's own configuration. Echoing the platform's value back as though the station had confirmed it would launder a guess into a measurement.\n\nOmitting the field means 'nothing to say' and leaves the stored position alone - a station that predates this sends nothing, and reading that as a retraction would take the map away from every station in the field. Retracting is explicit and is `null`.",
          "properties": {
            "latitude": {"type": "number", "minimum": -90, "maximum": 90},
            "longitude": {"type": "number", "minimum": -180, "maximum": 180},
            "elevation_m": {
              "type": "number",
              "minimum": -500,
              "maximum": 9000,
              "description": "Metres. Part of the position and stored with it. Optional - a station that cannot measure its height omits it, and omitting is not retracting, so the stored value stands. The ADS-B barometric correction is its consumer and refuses without one rather than assuming sea level."
            },
            "source": {
              "type": "string",
              "description": "How the station came by this: `configured` for a position typed at the box, `gps` for one a receiver fixed. Carried so the platform can tell a typed position from a surveyed one the day a GPS is fitted, rather than having to guess which it is looking at."
            }
          },
          "required": ["latitude", "longitude"]
        },
        "cadence": {
          "type": "object",
          "additionalProperties": {
            "type": "number",
            "exclusiveMinimum": 0
          },
          "description": "Seconds between publishes, per telemetry kind, as this station is actually running them. Consumers deriving a staleness timeout must use this rather than the defaults in transport.md: the weather period is a site setting, and a station legitimately slowed down to save bandwidth would otherwise be marked faulty by a console still assuming the default. Absent on an older agent, which means the defaults."
        },
        "uplink": {
          "type": "object",
          "description": "The station's view of its own link. `dropped_frames` counts telemetry deliberately discarded rather than queued - the contract asks for that, so it is a measure of an outage's length, not of a fault.",
          "properties": {
            "connected": {
              "type": "boolean"
            },
            "dropped_frames": {
              "type": "integer",
              "minimum": 0
            },
            "offline_seconds": {
              "type": "number",
              "minimum": 0
            }
          }
        },
        "credential": {
          "type": "object",
          "description": "Renewal health. A station that has stopped renewing is the failure that costs a site visit (enrolment.md section 6), and it is visible here long before the credential actually expires.",
          "properties": {
            "expires_at": {
              "type": "string"
            },
            "renewal_failures": {
              "type": "integer",
              "minimum": 0
            }
          }
        },
        "devices": {
          "type": "array",
          "maxItems": 64,
          "description": "What is actually attached, as the station found it. The platform reconciles its own inventory against this rather than assuming: a camera that has failed and a camera that was never fitted look identical in a database and completely different at the site. Bounded: a station has one slot per job and a handful of jobs.",
          "items": {
            "$ref": "#/$defs/device"
          }
        },
        "video": {
          "type": "object",
          "description": "The camera and the live stream. **This is where a console learns a station's camera state** - there is no video telemetry channel (transport.md) - and it is where `video.start` and `video.stop` report their effect, so unlike the diagnostics below it is specified rather than free-form. A consumer may depend on everything named here.",
          "required": [
            "stream"
          ],
          "properties": {
            "stream": {
              "type": "object",
              "description": "The live stream, and the reported effect of `video.start` / `video.stop`. A `video.start` that silently did nothing must be visible here: a station with no camera answers `state: \"unavailable\"` with a reason rather than going quiet, because quiet is what a wedged encoder looks like.",
              "required": [
                "state"
              ],
              "properties": {
                "state": {
                  "enum": [
                    "idle",
                    "starting",
                    "streaming",
                    "unavailable"
                  ],
                  "description": "`idle` - a camera is fitted and nobody has asked. `streaming` - sending now. `unavailable` - it cannot stream, and `reason` says why. The distinction that matters is idle against unavailable: one is a working station nobody is watching, the other needs somebody."
                },
                "reason": {
                  "type": "string",
                  "maxLength": 200,
                  "description": "Why it is not streaming, when it is not. For an operator."
                },
                "viewers": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "How many the platform has told it about, through repeated `video.start`."
                },
                "since": {
                  "type": ["string", "null"],
                  "description": "When the current stream started, ISO 8601. Null when not streaming."
                },
                "lease_remaining_s": {
                  "type": "number",
                  "minimum": 0,
                  "description": "Seconds before this stream stops on its own unless renewed. Zero when not streaming. A console watching this fall and not reset is watching the platform fail to renew."
                },
                "codec": {
                  "type": ["string", "null"],
                  "description": "The codec string sent on the media socket, derived from the bitstream: `avc1.PPCCLL` or `hvc1.P.C.TLL.CC`. Reported so a console can tell before playing whether this browser can decode it."
                },
                "frames": {"type": "integer", "minimum": 0},
                "dropped": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Fragments discarded because the link could not carry them. Rising is a bandwidth problem, not a camera fault - the station drops rather than queues, per transport.md."
                },
                "bitrate_bps": {"type": "integer", "minimum": 0},
                "fps_measured": {
                  "type": "number",
                  "minimum": 0,
                  "description": "What the encoder actually achieved, which is not always what was asked for. The gap between this and the requested rate is the difference between a hardware and a software encoder, and it is information rather than a fault."
                }
              }
            },
            "camera": {
              "type": "object",
              "description": "The sensor and how it is being read.",
              "properties": {
                "fitted": {"type": "boolean"},
                "backend": {
                  "type": ["string", "null"],
                  "description": "Which capture path is in use. Named rather than inferred because a slow path and broken hardware look identical from a console, and the difference is a packaging problem somebody can fix."
                },
                "backend_reason": {
                  "type": ["string", "null"],
                  "description": "Why that path and not a better one. This is the field that saves an SSH session."
                },
                "encoder": {
                  "type": ["string", "null"],
                  "description": "Hardware block or software, probed at start-up rather than assumed, so moving between boards is a setting and not a rewrite."
                }
              }
            },
            "sensor": {
              "type": "object",
              "description": "Who holds the camera right now. One sensor, one owner: a camera that is *busy* and a camera that is *broken* looked identical from the platform for the whole life of a bug that survived four fixes, and this is the field that separates them.",
              "properties": {
                "holder": {
                  "type": ["string", "null"],
                  "description": "The named holder, or null when nobody has it."
                },
                "held_for_s": {"type": "number", "minimum": 0},
                "grants": {"type": "integer", "minimum": 0},
                "refusals": {
                  "type": "integer",
                  "minimum": 0,
                  "description": "How often somebody asked and was refused. Climbing means two things want one camera."
                }
              }
            },
            "snapshots": {
              "const": false,
              "description": "Always false. The periodic snapshot channel was removed - two readers of one sensor was the camera wedge - and this says so out loud rather than leaving a console to infer it from a bitrate of zero, which is also what a broken camera looks like."
            }
          }
        },
        "resources": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "type": "object",
            "required": ["id", "kind"],
            "properties": {
              "id": {
                "type": "string",
                "maxLength": 100,
                "description": "Stable identity for this piece of hardware. Keyed on serial, never on enumeration order: two identical dongles swap indices between boots, and a slot bound to an index reads the other one's traffic."
              },
              "kind": {
                "type": "string",
                "maxLength": 40,
                "description": "What sort of thing it is - `rtlsdr`, `serial`."
              },
              "serial": {"type": "string", "maxLength": 100},
              "model": {"type": "string", "maxLength": 100},
              "detail": {"type": "string", "maxLength": 200}
            }
          },
          "description": "Physical hardware the station's device slots draw on, found rather than configured. Reported so a console can say 'two receivers configured, one dongle fitted' as a single shortage instead of showing two unrelated faults - which is what a slot-by-slot view does when several slots want one contended device."
        },
        "security": {
          "type": "object",
          "description": "Which link protections are actually active. Reported because \"is that station's traffic encrypted, and against which root\" should not require a shell on the box or a packet capture, and because the answer being *no* is exactly the case nobody would think to check.\n\n**No secret ever appears here.** `broker_url` is redacted of any credential before it travels.",
          "properties": {
            "broker_url": {
              "type": ["string", "null"],
              "description": "Where this station publishes, with any credential redacted."
            },
            "broker_tls": {
              "type": ["boolean", "null"],
              "description": "Whether the broker connection is encrypted. False on a station that should be publishing is a fault worth acting on immediately."
            },
            "platform_tls": {"type": ["boolean", "null"]},
            "publishing": {
              "type": "boolean",
              "description": "Whether the uplink is up at all. A station can be sensing, recording and alerting locally while this is false - it is cut off, not broken."
            },
            "tls_failed": {
              "type": "boolean",
              "description": "The station refused to connect because verification failed. It does not fall back, so this is a station that is deliberately silent rather than one that quietly downgraded."
            },
            "trust": {
              "$ref": "#/$defs/trust",
              "description": "The broker's root. Always pinned unless the platform said otherwise."
            },
            "api_trust": {
              "$ref": "#/$defs/trust",
              "description": "The platform API's root, which is a separate question - normally the system store, because the API is expected behind a public certificate."
            }
          }
        },
        "clock": {
          "type": "object",
          "description": "What is keeping this station's clock. A remote site with a wrong clock cannot authenticate and, if its credential has expired, cannot renew either - that is the failure that costs a site visit (enrolment.md section 6), so this is the early warning for it and not a curiosity.",
          "properties": {
            "synchronised": {
              "type": ["boolean", "null"],
              "description": "Whether the time source considers itself locked. Null means the station could not tell, which is not the same as no - and both are worth showing differently from yes."
            },
            "source": {
              "type": "string",
              "description": "What is disciplining it: `ntp`, `gps`, `rtc-only`, `none`, `unknown`. `rtc-only` is a box holding time across a reboot with nothing correcting drift; `none` is a box whose clock is whatever it was when it booted."
            },
            "detail": {
              "type": "string",
              "maxLength": 500,
              "description": "One line for a person."
            },
            "rtc": {
              "type": "boolean",
              "description": "Whether a battery-backed clock is fitted. On hardware without one this is the difference between a reboot losing minutes and losing the date entirely."
            }
          }
        },
        "unsourced_streams": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Telemetry kinds the station has no source for. The same fact as `available: false` on those streams, gathered in one place."
        },
        "unsourced_fields": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {"type": "string"},
            "maxItems": 64
          },
          "description": "Per telemetry kind, the field names the fitted instrument cannot measure - `{\"weather\": [\"humidity_pct\"]}` for an Airmar without the RH module. Distinct from an unsourced *stream*: the sensor is present and working and simply does not produce that value, so the console strikes the reading through rather than showing the dashes it shows while waiting."
        },
        "storage": {
          "type": "object",
          "description": "What the station is holding locally. It keeps recording and recording through an outage, so these numbers rising while `uplink.connected` is false is the system working, not failing.",
          "properties": {
            "events": {
              "type": "integer",
              "minimum": 0,
              "description": "Events on local storage."
            },
            "events_pending": {
              "type": "integer",
              "minimum": 0,
              "description": "Recorded and not yet acknowledged by the platform - the backlog on `gsu/{station_id}/events`. Falls to zero as the platform acknowledges. Persistently non-zero on a station whose uplink is up is a delivery fault worth chasing."
            },
            "recordings": {"type": "integer", "minimum": 0},
            "recordings_mb": {"type": "number", "minimum": 0}
          }
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/availability"
        }
      ]
    },
    "trust": {
      "type": "object",
      "description": "How one connection is verified. There are two of these and they are reported separately, because 'which CA is this box trusting' has two answers and merging them is what produced a station pinning the broker's private CA against an API behind a public certificate.",
      "properties": {
        "mode": {
          "enum": ["pinned", "system"],
          "description": "`pinned` - this CA and no other issuer, which is stronger than public PKI rather than weaker. `system` - the OS trust store, correct when the far end is behind a publicly trusted certificate."
        },
        "source": {
          "type": "string",
          "maxLength": 100,
          "description": "Where the root came from: the enrolment response, a pre-provisioned file, or the system store. A station that cannot say where its trust came from cannot be audited."
        },
        "fingerprint": {
          "type": ["string", "null"],
          "description": "SHA-256 of the pinned certificate, so a person can compare what the box trusts against what was issued without extracting anything from it. Null when the mode is `system`."
        },
        "require_tls": {
          "type": "boolean",
          "description": "Whether this station refuses plaintext outright."
        }
      }
    },
    "device": {
      "type": "object",
      "description": "One device slot, fitted or not.",
      "properties": {
        "slot": {
          "type": "string",
          "description": "What the device is for - adsb, radio, weather, power, light, camera. One slot per job; which product fills it is type_id."
        },
        "type_id": {
          "type": "string"
        },
        "label": {
          "type": "string",
          "description": "For an operator, not a parser."
        },
        "connection": {
          "type": "string"
        },
        "configured": {
          "type": "boolean",
          "description": "Someone said this device should be here."
        },
        "detected": {
          "type": "boolean",
          "description": "The station actually found it. Kept separate from `configured` on purpose: intent and reality are different facts, and conflating them is how a failed sensor reads as an absent one."
        },
        "driver_available": {
          "type": "boolean"
        },
        "status": {
          "type": "string",
          "enum": [
            "present",
            "not_fitted",
            "configured_absent",
            "stalled",
            "unsupported"
          ],
          "description": "The one field a console should branch on. `not_fitted` - nothing was ever meant to be here. `configured_absent` - it was specified and cannot be found. `stalled` - it was found and has stopped answering. The last two are faults; the first is not, and showing them the same way teaches operators to ignore all three."
        },
        "detail": {
          "type": "string"
        },
        "simulated": {
          "type": "boolean",
          "description": "Synthetic rather than real hardware. The platform badges a station DEMO from its own record; this says which *device* is pretending, which a mixed station needs."
        },
        "provides": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "absent": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "telemetry_kind": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "slot",
        "status"
      ]
    }
  }
}
